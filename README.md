*로봇에게 내리는 실제 명령값은 `size`(와 앞으로 추가될 `position`)이고, `furniture`(가구 이름)는 그 형태를 맥락에 맞게 표현한 자유 라벨.* 같은 size 0짜리 낮은 로봇이라도 엉덩이 밑이면 "낮은 의자", 발 밑이면 "발받침대", 옆이면 "협탁"이 된다.

---

## 동작 방식 (현재 파이프라인)

음성 한 번 → 로봇 4대의 명령 한 세트. 흐름은 다음과 같습니다.

1. **녹음** — 스페이스바를 누르고 있는 동안 마이크로 음성을 녹음한다.
2. **STT** — Groq Whisper(`whisper-large-v3-turbo`)로 한국어 음성을 텍스트로 전사한다.
3. **의도 분석** — OpenAI 호출로 발화에서 `number`(인원), `situation`(상황), `activity`(행동), `furniture`(필요 가구)를 구조화 추출한다.
4. **행동 생성** — 의도 + 최근 history를 입력으로, 로봇 4대(L BOT 1·2, H BOT 1·2)의 명령(`active`, `furniture`, `size`)을 생성한다.
5. **검증** — `size`를 [0, 25, 50, 75, 100] 단계로 스냅하고, 빈 라벨이면 inactive로 되돌리는 결정론적 안전 검사를 거친다.
6. **history 저장** — 검증을 통과한 명령만 누적한다. **history의 가장 최근 항목이 곧 현재 로봇 4대의 상태**다.

LLM 응답이 실패하거나 깨지면 로봇이 멋대로 움직이지 않도록 **4대 모두 inactive 기본값**을 반환한다.

### 로봇 하드웨어 모델

| 축 | 설명 |
| --- | --- |
| 높이 (종류) | `L BOT`은 항상 낮고, `H BOT`은 항상 높다. size로 높이는 바뀌지 않는다. |
| size (0~100) | 윗면 형태를 결정. 0=좁고 평평, 중간(25·50·75)=가운데가 패인 그릇(작을수록 깊은 바구니, 클수록 넓고 얕은 트레이), 100=크고 평평한 원형 면(테이블). |

**가용 재고는 L BOT 2대 + H BOT 2대, 총 4대뿐이다.** 가구가 더 필요해 보여도 이 4대를 초과할 수 없으며, 수요가 넘치면 우선순위를 정해 한정된 4대 안에서 최선의 조합을 구성한다.

---

## 실행 방법

```bash
# 1) 의존성 설치
pip install openai groq SpeechRecognition python-dotenv pyaudio

# 2) 환경 변수 설정
cp .env.example .env
#  .env 파일을 열어 GROQ_API_KEY, OPENAI_API_KEY 를 채운다.

# 3) 실행 (Windows — 스페이스바 키 감지에 ctypes 사용)
python STT_sr_button_groq_openai.py
```

스페이스바를 누른 채로 말하고, 떼면 전사·분석·명령 생성이 실행됩니다. `Ctrl+C`로 종료합니다.

> 참고: 현재 스페이스바 감지는 `ctypes.windll`을 사용하므로 **Windows 전용**입니다.

---

## 로드맵 (개발 현황)

이 프로젝트는 다음 세 방향으로 발전 중입니다.

### 1. 위치 스키마 추가 — *예정*
지금은 `size`만 명령하지만, **중심점 (0,0)을 기준으로 가구를 알맞은 위치 좌표에 배치**하는 축을 추가한다.
- (0,0)은 사용자 위치라기보다 **가구 세트를 배치할 중심점**이다. 이 중심을 기준으로 위·아래·좌·우를 구분한다.
- 위치까지 결정되면 "같은 로봇이 위치에 따라 의자/발받침대/협탁이 된다"가 라벨이 아니라 **좌표에서 자연스럽게 유도**된다.
- 함께 검토: 로봇 윗면 방향(yaw), 좌표계·단위 명시, **충돌·도달가능성 검증 레이어**(LLM이 아닌 결정론적 검사로 두 로봇이 겹치는 배치를 차단).

### 2. LLM Agent 전환 + LangSmith 트레이싱 — *예정*
현재의 단순 LLM 호출 방식을 **tool을 호출할 수 있는 에이전트 방식**으로 전환하고, **LangSmith**에 연결해 코드 흐름·추론 과정을 추적한다.
- 안전·충돌·재고 검증은 에이전트가 아니라 **결정론적 레이어가 최종 책임**진다.
- 실시간 제어인 만큼 지연·비결정성을 관리한다(하이브리드 구성 검토).

### 3. 라이브러리 + 툴 구축 — *예정*
에이전트가 참고/점검할 수 있는 지식 라이브러리와 조회 툴을 만든다.
- `furniture_mapping`: 활동 → 적절한 가구 조합 기준표(예: 독서 = 높은 카트 + 의자, 식사 = 큰 테이블 + 의자 여러 개). 강제 규칙이 아닌 **참고용 retrieval**로 두어 자유 라벨 철학을 유지한다.
- 위치 라이브러리: 신체 구역/방향 → 좌표 변환.
- 그 외 재고 조회, 현재 상태 조회, 레이아웃 검증 등 툴.
- 지식(데이터, JSON/YAML)과 툴(함수)을 분리해 git에서 버전 관리하기 쉽게 한다.

> 에이전트 도입으로 비결정성이 생기므로, **발화 → 기대 배치 평가셋**을 만들어 변경 시 회귀를 점검할 예정.

---

## 변경 이력 (Changelog)

- `main()` 루프 구조 정리: `text`가 비었을 때 `result_intent` 미정의로 발생하던 `NameError` 수정(각 단계 실패 시 `continue`로 안전 처리).
- 행동 생성 프롬프트에 **재고 제약(L BOT 2 + H BOT 2 = 총 4대) 명시**.
- 초기 저장소 구성: `README.md`, `.gitignore`, `.env.example` 추가.

